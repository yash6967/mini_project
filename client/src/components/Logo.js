import React from 'react';
import styled from 'styled-components';

const LogoText = styled.h1`
  font-size: 1.5rem;
  font-weight: bold;
  color: #ffffff;
  margin: 0;
`;

const Logo = () => {
  return <LogoText>Loan Agent Trainer</LogoText>;
};

export default Logo; 